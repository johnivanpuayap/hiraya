// Mock for server-only module in test environment
// In production (Next.js), server-only enforces boundaries at build time
// In tests (Node env), we allow it so we can test server-only functions
export {};
